package com.hathway.medbuddy.util

object TargetType {

    /*  fun  getTargetType(String : minTarget,minTarget){

          val isAfterMeal = label.contains("After", ignoreCase = true) || label.contains(
              "(AL)", ignoreCase = true

          val (minTarget, minTarget) = if (isAfterMeal || isBedtime) 70 to 140 else 70 to 100

      }*/
}